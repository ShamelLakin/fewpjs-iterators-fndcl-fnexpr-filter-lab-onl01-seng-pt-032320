const findMatching = (drivers) => {
  drivers.filter( drive => {

 })
}
 

// return  drivers.filter( driver => {
//     return (driver.split(' ').filter(word => {
//         return word.charAt(0).toUpperCase() + word.slice(1);
//     })).join(' ') 
//  }) 